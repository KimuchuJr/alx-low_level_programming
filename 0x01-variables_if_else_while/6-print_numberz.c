#include <stdio.h>

/**
 * main - Entry point
 * Description: Print number 0 - 9
 * Return: 0 (success)
 */
int main(void)
{
	for (num = 0; num <= 9; n++)
		putchar((char)num);
	putchar('\n');
	return (0);
}
