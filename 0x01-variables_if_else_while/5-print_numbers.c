#include <stdio.h>

/**
 * main -Entry point
 * Description - Print all single digits numbers of base 10, starting from o
 * Return: 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
		printf("%d", num);
	printf("\n");
	return (0);
}
